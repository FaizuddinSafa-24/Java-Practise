long second = timeISeconds / 1000;
        // long minute = (long) second / 60;
        // long hr = minute / 60;
        // System.out.printf("miliseconds: %f, seconds = %f, minutes= %d, hours = %d", timeISeconds, second, minute, hr);
